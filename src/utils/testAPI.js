/* import axios from 'axios';

const testAPI = async () => {
  try {
    const res = await axios.get(`${import.meta.env.VITE_API_BASE_URL}/auth/test`);
    console.log('✅ Axios connected:', res.data);
  } catch (err) {
    console.error('❌ Axios error:', err.message);
  }
};  

export default testAPI; */
